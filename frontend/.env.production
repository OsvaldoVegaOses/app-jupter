# Produccion recomendada: same-origin via nginx (/api -> BACKEND_URL).
# Dejar vacio para que el browser use rutas relativas y evitar CORS/cookies cross-site.
VITE_API_BASE=
# Solo para Vite dev server (no usado por el browser en build de produccion).
VITE_BACKEND_URL=
