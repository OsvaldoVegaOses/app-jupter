# Dev Dockerfile for Vite frontend
FROM node:20-alpine

WORKDIR /app

# Install dependencies first (better cache reuse)
COPY package*.json ./
RUN npm ci

# Copy source
COPY . .

EXPOSE 5173

# Run Vite dev server on all interfaces
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "5173"]
